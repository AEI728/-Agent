# 开发阶段默认使用模拟回答；接入正式 API 前将其改为 false，并填写真实密钥。
USE_MOCK_LLM=true
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
PORT=4311

# 知识库预留路径：当前不放入任何知识文件。后续可将 .md/.txt/.json 文档放入该目录，
# 或通过 POST /api/knowledge/import 导入外部知识源。
KNOWLEDGE_BASE_PATH=./knowledge-base
RAG_TOP_K=4
